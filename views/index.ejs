<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .all-content{
            margin: 100px 300px;
        }
        
        .img{
            width: 200px;
            height: 100px;
        }

        .flexbox{
            display: flex;
            flex-direction: row;
            flex-wrap: wrap;
            justify-content: flex-start;
            align-items: stretch;
            box-sizing: border-box;
            /* margin: 0px -10px; */
            }

            .item{
            box-sizing: border-box;
            width: 33%;
            height: 100%;
            margin-bottom: 50px;
            padding: 0 50px;
            align-items: center;
            justify-content: center;
            border-radius: 10px;
            }

            .content{
            color: rgb(0, 0, 0);
            /* background-color: #989999; */
            background-color: rgb(143, 142, 142);
            box-shadow: 2px 2px 16px 13px rgba(217, 217, 217, 0.1);
            box-shadow: 1;
            font-weight: 600;
            text-align: center;
            box-sizing: border-box;
            height: 100%;
            padding: 5px;
            z-index: 999;
            align-items: center;
            justify-content: center;
            height: 200px;
            padding-top: 30px;
            border-radius: 5px;
            display: flex;
            flex-direction: column;
        }
    </style>
</head>
<body>
    <%- include('navbar') %>
    <div class="all-content">
        <div class="flexbox">
            <% Object.values(product).forEach((item)=>{%>
                <div class="item">
                    <div class="content">
                        <div><img class="img" src="/images/blogimg/<%= item.image %>" /></div>
                        <div>
                            <%= item.title%>
                            <%= item.date%>
                            <%= item.description%>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>